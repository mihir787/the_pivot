<div class="index-container">

<h2>All Menu Photos</h2>

  <% @photos.each do |photo| %>
      <div class="col-lg-3 col-md-4 col-sm-6">
        <%= image_tag photo.image.url(:thumb) %>
      <h4><%= link_to "#{photo.title}",photo_path(photo) %></h4>
    </div>
  <% end %>
</div>
