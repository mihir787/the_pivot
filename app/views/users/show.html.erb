<div class="user-show">

<h1><%= "#{current_user.name} Dashboard" %></h1>
<ul>
<li><%= image_tag current_user.picture.url(:small) %></li>
<li><%= button_to "Edit Profile", edit_user_path, method: :get %></li>
<li><%= button_to "Delete", user_path, method: :delete %></li>

<%= button_to "Order History", user_orders_path(user_id: current_user.id), method: :get %>
</ul>
</div>
